name: Deploy BlueOS Extension Image

on:
  push:
  workflow_dispatch:

permissions:
  contents: write

jobs:
  deploy-docker-image:
    runs-on: ubuntu-latest
    steps:
      - name: Clear Docker cache
        run: |
          docker builder prune -f
          docker buildx prune -f

      - name: Deploy BlueOS Extension
        uses: BlueOS-community/Deploy-BlueOS-Extension@v1.2.0
        with:
          docker-username: ${{ secrets.DOCKER_USERNAME }}
          docker-password: ${{ secrets.DOCKER_PASSWORD }}
          github-token: ${{ secrets.GITHUB_TOKEN }}
          # Final image is <docker-username>/blueos-rtk-basestation
          # (the action prepends the default "blueos-" prefix).
          image-name: "rtk-basestation"
          build-platforms: "linux/arm64/v8,linux/arm/v7"
          author: vshie
          maintainer: vshie
